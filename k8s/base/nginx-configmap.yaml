apiVersion: v1
kind: ConfigMap
metadata:
  name: nginx-html-config
data:
  index.html: |
    <html>
    <body>
      <h1>Hello from ConfigMap! 🎉</h1>
      <p>This page is NOT baked into the nginx image - it's mounted from a ConfigMap.</p>
    </body>
    </html>
